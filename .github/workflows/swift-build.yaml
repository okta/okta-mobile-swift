name: Package Managers

on:
  push:
    branches:
      - master
      - dev-*
      - release-*
  pull_request:
    branches: 
      - master
      - dev-*
      - release-*

env:
  DEVELOPER_DIR: /Applications/Xcode_13.3.app/Contents/Developer

jobs:
  SwiftBuild:
    name: Swift Package Manager on ${{ matrix.OS }}
    runs-on: ${{ matrix.OS }}
    strategy:
      matrix:
        OS:
          - macos-12
    steps:
    - name: Get swift version
      run: swift --version
    - uses: actions/checkout@master
    - name: Build
      run: swift build --build-tests
    - name: Test
      run: swift test
  XcodeBuild:
    name: Xcode
    runs-on: macos-12
    steps:
    - uses: actions/checkout@master
    - name: Build
      run: set -o pipefail && env NSUnbufferedIO=YES xcodebuild build-for-testing -scheme AuthFoundation -destination "platform=iOS Simulator,OS=latest,name=iPhone 12" | xcpretty
    - name: Test
      run: set -o pipefail && env NSUnbufferedIO=YES xcodebuild test-without-building -scheme AuthFoundation -resultBundlePath TestResults.xcresult -destination "platform=iOS Simulator,OS=latest,name=iPhone 12" | xcpretty
    - uses: kishikawakatsumi/xcresulttool@v1
      with:
        path: TestResults.xcresult
      if: success() || failure()
